#include "main.h"

/**
 * _abs - computes the absolute value of a number given
 * @i: the number to be checked
 * Return: the absolute value of number
 */
int _abs(int i)
{
	if (i >= 0)
	{
		return (i);
	}
	return (-i);
}
